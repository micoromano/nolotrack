# Log esecuzione SP3 + SP1 — 2026-05-15

## SP completati

- **SP3** — Tema Chiaro/Scuro ✅
- **SP1** — Form Corse Potenziata ✅

---

## Commit hashes

| Commit | Descrizione |
|---|---|
| `736e882` | deps: aggiungi next-themes |
| `b73488f` | feat: aggiungi ThemeProvider con next-themes |
| `07259b4` | feat: integra ThemeProvider nel layout root |
| `08b9b69` | feat: aggiungi variabili CSS tema chiaro |
| `063058b` | feat: aggiungi toggle tema nella navbar |
| `8bb5487` | db: aggiungi campi ordine servizio alla tabella corse |
| `1a36fce` | types: aggiungi campi ordine servizio a interface Corsa |
| `11a7346` | feat: aggiungi sezione dettaglio ordine alla form nuova corsa |
| `e943d75` | feat: aggiungi colonne cliente e pax alla lista corse |

---

## Passi manuali richiesti (utente)

### Migrazione SQL — obbligatoria prima di usare SP1

1. Aprire il **Supabase dashboard** → **SQL Editor**
2. Aprire il file `supabase/migrations/20260515_sp1_corse_fields.sql`
3. Incollare il contenuto nel SQL Editor e premere **Run**
4. Verificare con:
   ```sql
   SELECT id, n_ordine, anno_ordine, cliente_nome, n_pax FROM corse LIMIT 5;
   ```
   Le nuove colonne devono esistere e `n_pax` deve avere valore 1.

---

## Note / Problemi

- Nessun problema riscontrato. TypeScript compila senza errori dopo tutte le modifiche.
- La directory `supabase/migrations/` è stata creata (non esisteva).
- SP3: il toggle tema appare nella sidebar desktop (select con testo) e nell'header mobile (select con emoji). `defaultTheme="dark"` garantisce che il tema scuro sia attivo al primo accesso.
- SP1: la form corse mostra la sezione "Dettaglio ordine" con 7 nuovi campi. La lista corse desktop ora ha 6 colonne (aggiunta Cliente/Pax). La migrazione SQL **non** è stata eseguita automaticamente — va lanciata manualmente nel dashboard Supabase.
